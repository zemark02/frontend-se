import { Layout } from "../../component/layout";
import SearchForm from "./SearchForm";

const Search = () => {
  return (
    <Layout page="Search">
      <SearchForm />
    </Layout>
  );
};

export default Search;
